_target_: src.data.lrs_datamodule.LRSDataModule
data_root: ["${paths.lrs3_root}"]
audio_root: ["${paths.lrs3_audio_root}"]
video_root: ["${paths.lrs3_video_root}"]
speaker_emb_root: ["${paths.lrs3_speaker_emb_root}"]
face_emb_root: ["${paths.lrs3_face_emb_root}"]
background_noise_root: ${paths.background_noise_root}
batch_size: 40
num_workers: 12
pin_memory: True
channels: 1
speakers: 2
max_data: -1
load_features: True
load_speaker_embedding: True
persistent_workers: True
max_duration: 4
transform: False
add_background_noise: False
add_pink_noise: False
generate_speaker_dict: True
positive_embeddings: 3
negative_embeddings: 2
