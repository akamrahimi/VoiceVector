_target_: src.data.librispeech_datamodule.LibriSpeechDataModule
data_root: ["${paths.librispeech_root}"]
audio_root: ["${paths.librispeech_audio_root}"]
speaker_emb_root: ["${paths.librispeech_speaker_emb_root}"]
background_noise_root: ${paths.background_noise_root}
batch_size: 40
num_workers: 12
pin_memory: True
channels: 1
speakers: 2
max_data: -1
load_speaker_embedding: True
persistent_workers: True
max_duration: 5 
transform: False
add_background_noise: False
generate_speaker_dict: True
positive_embeddings: 3
negative_embeddings: 3
